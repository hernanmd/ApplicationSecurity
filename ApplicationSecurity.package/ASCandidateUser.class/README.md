An user which is candidate to be an ASUser. This is for example a user which is registering.

The registrationId is an unique identifier assigned when the candidate asks for registration. It should be expired after a configured duration.
